#ifndef nomad__src__meta__traits_hpp
#define nomad__src__meta__traits_hpp

namespace nomad {
  
  /*
  // Compute the maximum size of a list of vectors
  template <typename T>
  int max_size(T& v) {
    return size_of(v);
  }
  
  template <typename Head, typename… Tail>
  int max_size(Head& head, Tail…& tail) {
    int max_tail = max_size(tail);
    return size_of(head) > max_tail ? size_of(head) : max_tail;
  }
  */
  
}

#endif
